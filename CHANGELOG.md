# Changelog

后续变更按 [Conventional Commits](https://www.conventionalcommits.org/) 记录，版本遵循语义化版本号。新仓库从 0.3.0 开始记录公开 Git 历史；下面的 0.2.3 是依据现有验收记录人工整理的历史摘要，不代表仓库中存在对应提交或标签。

## [Unreleased]

暂无记录。

## [0.4.1] - 2026-09-23

### Fixed

- 自动拖动时短暂等待系统处理已发送事件，避免把仍停留在上一个拖动点的正常延迟误判为鼠标干扰；保留有界超时、外部输入检查及释放逻辑。
- 区分拖动事件超时与其他取消原因，应用期间持续显示进度和暂停鼠标键盘操作的提示。
- 图形管理的完成操作改为「应用并收起」，全部分类确认后直接收起；失败则明确显示尚未全部生效，并保持全部展开。

## [0.4.0] - 2026-09-23

### Added

- 菜单栏右键新增原生 ⌘ 拖拽分组模式，只在整理期间显示「常隐」「收起」边界；直接点击「···」即可完成并收起。
- 连续两次读取可信位置后同步手动分类，保留未确认项目及界面中的显式草稿。原生常隐分区按物理边界启用，避免没有稳定标识的项目在普通展开时意外显示。
- 管理界面提供对应拖拽指引、完成和保持展开退出入口；整理期间暂停自动收起及冲突的列表操作。

### Fixed

- 修复右键「临时显示全部」意外打开管理窗口的问题，并增加菜单内的「结束临时显示」。
- 结束临时显示时恢复进入前的展开／收起状态，重复调用不会覆盖该状态。
- 拖拽模式在权限撤销或显示器恢复时退出并保持展开，取消过期扫描，避免接纳已失效的结果。

## [0.3.1] - 2026-09-23

### Fixed

- 在 CI 中将专用临时钥匙串加入当前用户搜索列表，修正 macOS 15 runner 已导入证书但 codesign 无法找到签名身份的问题。
- 构建前先执行实际签名与证书约束校验；清理时移除临时搜索项，保留原有钥匙串配置。

### Release

- 增加显式启用的手动签名验证任务，在打标签前验证双架构签名包，不创建 Release。
- 发布 Apple Silicon 和 Intel 两个原生预览包，包含 0.3.0 中的图形分组、权限引导、开发检查及自动发布流程。
- 保留未成功发布安装包的 `v0.3.0` 标签，使用新补丁版本修复发布流水线；已知 macOS 27 容量限制仍然适用。

## [0.3.0] - 2026-09-23

此标签的签名发布任务失败，未产生可下载的 GitHub Release；代码与 CI 记录保留，后续使用 0.3.1 发布。

### Added

- 整理为可独立构建的公开 Swift Package 项目，保留图形管理列表、常驻显示／收起后隐藏／始终隐藏三种分类、草稿及应用结果。
- 增加本地开发检查、Conventional Commit 校验、提交前静态检查和推送前 Swift 构建／测试 hooks。
- 增加 git-cliff 配置与只输出预览的变更记录脚本，保留首次发布的人工验收摘要。
- 增加 GitHub Actions：每次提交构建并测试 Apple Silicon 和 Intel 原生应用，推送版本标签后自动发布预览版。
- 发布包使用项目专用的固定签名身份，并提供 SHA-256 校验文件与包含源码提交、架构和签名要求的构建元数据。

### Fixed

- AX 元数据回退改为同一 actor 内顺序读取，修正 Swift 6.2 对闭包捕获非 Sendable AX 对象的隔离诊断；保留节点与前两个子项的原有读取优先级。

### Documentation

- 说明辅助功能授权、稳定安装路径、本地签名、构建产物与实际功能验收的区别。
- 明确 macOS 27 的菜单栏容量限制：三类移动及保存的通过证据，不代表普通展开能够显示全部溢出图标。
- 保留既有实机验收记录；新版本与打包流程不自动扩大已经验证的系统、显示器或异常处理范围。

## [0.2.3] - 2026-09-23

### Historical summary

- macOS 27 主屏 AX 扫描区分 MenuBarAgent 宿主和项目真实所属应用，去除镜像重复项，并保护系统 overflow 恢复入口。
- 管理期间按需打开系统 overflow 或保持已展开区域停驻，完成原生 ⌘ 拖动后验证真实位置；明显重叠的占位 frame 不作为可信分类依据。
- 在有界等待中连续两次确认可信位置才接受并保存分类；失败保留未完成草稿，管理清理释放合成输入并按归属恢复系统 overflow。
- 本机实测三种分类移动、保存、规则重启保留，以及成功路径的 overflow 和鼠标恢复；最终测试目标恢复常驻显示。
- 普通展开仍受 macOS 27 刘海屏可见容量限制，部分目标需手动打开系统 overflow。全局快捷键、全部异常清理路径和其他显示器拓扑没有因此获得完整验收。

详见 [0.2.3 实机验收记录](docs/ACCEPTANCE-0.2.3.md)。这些记录是历史运行证据，不代表新发布包已经重新完成同样的实机测试。
